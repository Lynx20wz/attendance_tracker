name: attendance_tracker
description: Student attendance tracker
version: 0.2.1
repository: https://github.com/Lynx20wz/attendance_tracker

environment:
  sdk: ^3.9.0

dependencies:
  path: ^1.9.0
  path_provider: ^2.1.5
  flutter:
    sdk: flutter
  riverpod: ^3.2.1
  flutter_riverpod: ^3.2.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  import_sorter: ^4.6.0

flutter:
  uses-material-design: true
  assets:
    - assets/students.txt

import_sorter:
  comments: false
